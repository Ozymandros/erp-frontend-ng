# pnpm settings (pnpm version 10.28.1)
# Note: npm v11+ warns about unknown config keys in .npmrc.
# Removed redundant 'package-manager=pnpm' and 'auto-install-peers=true' (default in pnpm v10)
# to reduce npm warnings.

strict-peer-dependencies=false
